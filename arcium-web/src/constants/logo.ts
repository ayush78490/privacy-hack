// Logo path - import the image from src/assets folder
// Vite will process this and create a proper asset path in the build
import logoImage from '../assets/privypay.png';

export const LOGO_PATH: string = logoImage;
